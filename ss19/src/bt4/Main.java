package bt4;

public class Main {
    public static void main(String[] args) {
        Circle ci = new Circle(3);
        System.out.println("Area of Circle: " + ci.area());
       Shape.description();
    }
}
